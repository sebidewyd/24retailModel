#region Data
i = 1;
line = 'CubeCreate('|V1|'';
IF(SCAN( '}', V1 )=0);
max = CubeDimensionCountGet(V1);
while (i<=max); 
    TABDIM( V1, i );
    line = line|','|TABDIM( V1, i );


    i=i+1; 
end;
        ASCIIOutput( 'l.txt', line|')' );
    ENDIF;
#endregion